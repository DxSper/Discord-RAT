import os
from simple_term_menu import TerminalMenu

def main():
    terminal_menu = TerminalMenu(["obfuscate code", "compile python to exe", "run Dx-RAT"])
    terminal_menu = TerminalMenu(fruits, title="Dx-RAT Terminal")
    menu_entry_index = terminal_menu.show()


if __name__ == "__main__":
    main()
